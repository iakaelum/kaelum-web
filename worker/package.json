{
  "name": "kaelum-chat-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy entre el widget de chat de KAELUM y OpenRouter (Claude Haiku 4.5). Mantiene la API key segura fuera del frontend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
